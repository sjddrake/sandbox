package co.uk.sandbox;

public class CompileTest {

    private static final String SKILL_DELIMITER = "|";

    private static final String BACKSLASH = "\\";

    public static void main(final String[] args) {
        // TODO Auto-generated method stub

        final StringBuilder concatenatedSkills = new StringBuilder();
        final String delimiter = "";

        final String prefix = "";
        final String statementCode = "";

        concatenatedSkills.append(delimiter + prefix + BACKSLASH + statementCode);

        concatenatedSkills.append(delimiter).append(prefix).append(BACKSLASH).append(statementCode);

    }

}
